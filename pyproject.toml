[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "caesar"
version = "0.1.0"
description = "Interactive case-conversion clipboard utility"
dependencies = ["pyperclip"]

[project.scripts]
caesar = "caesar.main:main"
